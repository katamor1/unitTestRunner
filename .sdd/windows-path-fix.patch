diff --git a/tests/test_execution_run_history.py b/tests/test_execution_run_history.py
index 103a364..3361c53 100644
--- a/tests/test_execution_run_history.py
+++ b/tests/test_execution_run_history.py
@@ -85,12 +85,13 @@ class ExecutionRunHistoryTests(unittest.TestCase):
         with tempfile.TemporaryDirectory() as temp_dir:
             workspace = Path(temp_dir)
             paths = create_run_paths(workspace, run_id="run-blocked")
+            resolved_workspace = workspace.resolve()
             self.assertEqual(
-                workspace / "runs" / "run-blocked" / "test_execution_blockers.json",
+                resolved_workspace / "runs" / "run-blocked" / "test_execution_blockers.json",
                 paths.blocker_report_json,
             )
             self.assertEqual(
-                workspace / "runs" / "run-blocked" / "test_execution_blockers.md",
+                resolved_workspace / "runs" / "run-blocked" / "test_execution_blockers.md",
                 paths.blocker_report_markdown,
             )
 
diff --git a/scripts/build_distribution.ps1 b/scripts/build_distribution.ps1
index 9481d96..da224b0 100644
--- a/scripts/build_distribution.ps1
+++ b/scripts/build_distribution.ps1
@@ -239,7 +239,9 @@ try {
         "run-tests",
         "--workspace", $smokeRoot,
         "--run",
-        "--executable", $exePath,
+        # Use the workspace-relative default executable path. The smoke is
+        # intentionally blocked before execution, and contract artifacts must
+        # not record the distribution executable as an external absolute path.
         "--run-id", "release-blocked-smoke"
     )
     $blockerJson = Join-Path $smokeRoot "reports\test_execution_blockers.json"

diff --git a/tests/test_distribution_build_script.py b/tests/test_distribution_build_script.py
index 0c1dbdb..5575fa2 100644
--- a/tests/test_distribution_build_script.py
+++ b/tests/test_distribution_build_script.py
@@ -84,6 +84,14 @@ class DistributionBuildScriptTests(unittest.TestCase):
             with self.subTest(fragment=fragment):
                 self.assertIn(fragment, self.text)
 
+    def test_blocked_smoke_uses_workspace_relative_default_executable(self):
+        smoke_start = self.text.index('Invoke-NativeExpectedExit -FilePath $exePath -ExpectedExitCode 35')
+        smoke_end = self.text.index('$blockerJson =', smoke_start)
+        blocked_smoke = self.text[smoke_start:smoke_end]
+
+        self.assertNotIn('"--executable"', blocked_smoke)
+        self.assertIn('"--run-id", "release-blocked-smoke"', blocked_smoke)
+
     def test_script_rejects_non_windows_hosts_and_stops_on_native_failures(self):
         self.assertIn('$env:OS -ne "Windows_NT"', self.text)
         self.assertIn('$ErrorActionPreference = "Stop"', self.text)
