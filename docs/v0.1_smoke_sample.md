# v0.1スモークサンプル

このサンプルは `tests/fixtures/vc6_project` のVC6風fixtureを使い、本番fixtureツリーの外側に生成成果物を書き出します。

## 解析とテスト設計のCLIフロー

`Control_Update` を対象に、関数dossier生成、build probe dry-run、テスト設計生成を確認します。

```powershell
$env:PYTHONPATH = "$PWD\src"
$fixture = "$PWD\tests\fixtures\vc6_project"
$out = "$env:TEMP\unitTestRunner-smoke\Control_Update"

py -m unit_test_runner analyze-function `
  --workspace $fixture `
  --dsw "$fixture\Product.dsw" `
  --source src\control.c `
  --function Control_Update `
  --configuration "Win32 Debug" `
  --project Control `
  --out $out

py -m unit_test_runner build-probe --dossier "$out\reports\function_dossier.json" --dry-run
py -m unit_test_runner generate-test-design --dossier "$out\reports\function_dossier.json"
```

このフローは、`build-probe --dossier` と `generate-test-design --dossier` の公開入口が、通常解析で生成した `reports/function_dossier.json` を消費できることを確認するための最短経路です。

## 確定済みdossierレビューのCLIフロー

人間レビューまたはVS Code adapter経由で使う成果物を作る場合は、`analyze-function` に `--finalize-dossier` を付けます。

```powershell
py -m unit_test_runner --json analyze-function `
  --workspace $fixture `
  --dsw "$fixture\Product.dsw" `
  --source src\control.c `
  --function Control_Update `
  --configuration "Win32 Debug" `
  --project Control `
  --out $out `
  --finalize-dossier

py -m unit_test_runner --json finalize-dossier --workspace $out
py -m unit_test_runner --json prepare-review --dossier "$out\reports\function_dossier.json"
py -m unit_test_runner --json run-tests --workspace $out --dry-run
py -m unit_test_runner --json prepare-evidence --workspace $out
```

`function_dossier.json` は確定後も元の解析契約フィールドを維持し、レビュー用のフィールドを追加します。同じdossierは引き続き `build-probe --dossier` と `generate-test-design --dossier` に渡せます。

期待される主なレビュー成果物は以下です。

- `input/request.json`
- `extracted/src/control.c`
- `extracted/include/control.h`
- `generated/prompt_pack.json`
- `generated/build/Makefile`
- `reports/function_dossier.json`
- `reports/function_dossier.md`
- `reports/project_membership.md`
- `reports/build_context.json`
- `reports/source_file_set.md`
- `reports/global_access_report.md`
- `reports/call_report.md`
- `reports/branch_condition_report.md`
- `reports/coverage_design.md`
- `reports/boundary_equivalence_candidates.md`
- `reports/stub_candidates.md`
- `reports/test_case_design.csv`
- `reports/build_probe.log`
- `reports/dossier_manifest.json`
- `reports/traceability_matrix.csv`
- `reports/review_checklist.md`
- `reports/unresolved_items.md`
- `reports/next_actions.md`

## 実用解析fixture

`tests/fixtures/vc6_practical_project` は、解析回帰と手動レビュー向けの密なサンプルです。基本フローは同じですが、対象は `DeviceControl_Update` です。この関数周辺には、file-scope static、externグローバル、配列、構造体メンバ、callback/関数ポインタ呼び出し、`#define` 定数、別名、function-like macro が含まれています。

```powershell
$env:PYTHONPATH = "$PWD\src"
$fixture = "$PWD\tests\fixtures\vc6_practical_project"
$out = "$env:TEMP\unitTestRunner-practical\DeviceControl_Update"

py -m unit_test_runner analyze-function `
  --workspace $fixture `
  --dsw "$fixture\Product.dsw" `
  --source src\device_control.c `
  --function DeviceControl_Update `
  --configuration "DeviceControl - Win32 Debug" `
  --project DeviceControl `
  --out $out

py -m unit_test_runner build-probe --dossier "$out\reports\function_dossier.json" --dry-run
```
